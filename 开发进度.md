本文件用于记录开发进度，方便Claude Code确定当前进度和开发方向。

# 3月26日20:34

## 当前进展

1. 生成了4个Vehicle、3个Caravan的3D .obj格式模型。
2. 生成方式为Claude产生文字Prompt -> Nano Banana生成图片 -> Hunyuan 3D生成3D 模型

## 下一步计划

1. ~~使用相同方法生成后视镜3D模型。~~
2. 将上述3D模型导入Blender并存为.blender格式，根据文件保存为指定的size。注意这一步模型的比例不一定和config比例一致，vehicle和caravan和应该保持和config预设的宽度一致，~~后视镜尺寸应该按照长度一致~~。
3. 确认vehicle、caravan、~~mirror~~上的互相连接点。
4. 根据互相连接点链接并确认方向。

```将Assest文件夹的4个vehicle和4个caravan模型，分别导入blender，并添加完整材质。每个模型根据config和doc的参数，将模型尺寸设置为指定尺寸。如模型的比例不和config比例一致，应将模型宽度和config的宽度一致，整体比例保持模型原比例。之后存为.blender格式到Assest文件夹下blender-model文件夹内。```

```我关于后视镜模型的思考，请分析我思考是否有问题，并给出方案。如果建立一个完整后视镜模型（后视镜外壳，内部，镜面、连接等），其余部件和镜面不一定有完美的匹配，甚至存在穿模等风险，而除了镜面之外的部分，也无用于视野的仿真。因此是否可以简化后视镜模型，只保留镜面、和一个镜面外的安装点，这个安装点作为连接车身的参照和连接点，以及镜面朝向？```

~~思考场景问题~~（已完成）

5. 已完成 4 个场景的代码实现：
   - 场景1: 高速公路行驶（3车道，参考车辆）— `src/scenarios/highway.py`
   - 场景2: 停车场静态测试（雪糕桶+立杆网格）— `src/scenarios/parking.py`
   - 场景3: UN ECE R46 法规验证（可视区域标记+测试柱）— `src/scenarios/regulatory.py`
   - 场景4: 变道检测（多距离接近车辆）— `src/scenarios/lane_change.py`（新增）
   - 配置: `config/scenarios.yaml`
   - 入口: `src/scene_builder.py`
   - 辅助物体: `src/objects/`（参考车、雪糕桶、R46标记、道路系统）

```
制定计划，不同类型后视镜的搭建，主要建立后视镜，并给我审核，我同意后，保存在assest下的blender-mirror里的.blender文件下。
```

# 3月28日

## New Agent：用于统一场景搭建

```
当前agent用于统一各模型坐标关系。可以命名包含coordinator。
（原始任务描述省略，见 git history）
```

## 坐标统一 Agent 完成记录

### 已完成（3月28日）

1. **坐标系文档** `docs/coordinates.md` — 9节完整规范
   - 全局坐标系（Blender 原生 +X右 +Y前 +Z上）
   - 旋转约定（Euler XYZ + SAE yaw/pitch/roll 映射）
   - 车辆/房车/后视镜/场景各自本地坐标系与 bbox
   - 变换链公式 + 12 组车辆-房车定位表
   - 后视镜全局定位方法

2. **模型原点校准** — 全部 .blend 文件
   - 9 个后视镜：原点在镜面中心，+Z=法向量 ✓
   - 4 个车辆：原点在前轴中心地面投影 ✓
   - 3 个房车：原点在球头挂接点 (Z=0.42m) ✓
   - 4 个场景：验证通过 ✓

3. **模型朝向修复**（3月28日追加）
   - **Bug**: 所有 AI 模型视觉前端朝 -Y（反了）
   - **修复**: `scale.y=-1` Y轴翻转（保留左右关系），清除 OBJ 导入残留旋转
   - **结果**: 所有模型 rotation=(0,0,0) scale=(1,1,1)，视觉车头朝 +Y
   - **验证**: 组装 CR-V + 中型房车 + 后视镜 + 法规场景，侧视图/近景/标记点全部确认

4. **文档交叉检查**
   - `ProjectOverview.md` 坐标节精简 → 引用 coordinates.md
   - `MirrorDesign.md` 坐标节精简 → 引用 coordinates.md
   - `TestScene.md` 坐标节精简 → 引用 coordinates.md
   - `vehicles.yaml` / `caravans.yaml` 添加 coordinates.md 引用注释

### 当前状态

- 所有模型坐标系已统一，可直接加载组装
- `mirror_builder.py` 中 `create_both_mirrors()` 可自动创建左右镜面
- 场景组装已验证：车辆 + 房车 + 后视镜 + 道路场景全部正确对齐
- `validate_mirror_position()` 有已知 bug（用局部坐标而非世界坐标），不影响实际功能

# 3月29日

## 已完成

1. **后视镜左右差异化**
   - `mirrors.yaml` 重构：standard_convex、electric_main、towing_main 拆分为 left/right 子配置
   - 驾驶员侧（左）使用更大曲率半径（更平），副驾驶侧（右）使用更小曲率半径（更凸）+ 略大尺寸
   - 广角副镜(electric_sub, towing_wide_angle)和盲点镜(clip_on_blind_spot)左右相同
   - 删除 standard_flat（平面镜不符合现代标准）
   - 新增左右独立 .blend 文件：electric_main_L/R、standard_convex_L/R、towing_main_L/R

2. **镜面构建精度提升**
   - `mirror_builder.py` 使用精确球冠公式 `z = R - sqrt(R² - x² - y²)` 替代 Cast 修改器近似映射
   - 新增 `get_blend_filename()` 函数支持左右差异化文件名查找
   - 曲率半径误差 < 1%

3. **模型坐标校准提交**
   - 所有 .blend 模型文件已包含原点校准和 Y轴朝向修复
   - vehicles.yaml / caravans.yaml 添加坐标引用注释
   - 文档交叉引用 coordinates.md

4. **代码与配置清理**
   - `src/scenarios/base.py` 增强场景基类
   - `docs/MirrorDesign.md`、`ProjectOverview.md`、`TestScene.md` 精简坐标节

### 下一步

1. 修复 `validate_mirror_position()` 使用 `matrix_world.translation` 而非 `location`
2. 编写场景自动组装脚本（输入车型+房车+镜型，一键生成完整场景）
3. 实现镜面 FOV 覆盖范围计算
4. 批量仿真矩阵：遍历所有组合渲染

