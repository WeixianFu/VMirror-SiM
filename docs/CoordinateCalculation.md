# 坐标计算推导

> 本文档从 Blender 模型实测数据出发，逐步推导后视镜的位置、朝向和可视范围。
> 以 CR-V (SUV) 为算例，走通后推广到其他车型。

---

## 0. 符号约定

### 坐标系

采用 Blender 原生坐标系，全局原点 = 车辆前轴中心地面投影。

| 轴 | 方向 | 正方向 |
|----|------|--------|
| X | 横向 | 副驾侧（右） |
| Y | 纵向 | 车头（前进方向） |
| Z | 垂直 | 向上 |

### 符号表

| 符号 | 含义 | 单位 |
|------|------|------|
| $W$ | 车身宽度（不含后视镜） | m |
| $P_{\text{mount}}$ | 后视镜安装基准点 | (x, y, z) m |
| $P_{\text{glass}}$ | 镜面中心世界坐标 | (x, y, z) m |
| $P_{\text{eye}}$ | 驾驶员眼点 | (x, y, z) m |
| $P_{\text{target}}$ | 目标点（希望在镜中看到的点） | (x, y, z) m |
| $\hat{n}$ | 镜面法向量（单位向量） | - |
| $R$ | 凸面镜曲率半径 | m |
| $\theta_{\text{FOV}}$ | 视场角 | rad / ° |

---

## 1. 镜面位置推导

### 1.1 输入量：Blender 模型实测

对 CR-V 模型 (`node_0`) 进行测量，关键数据如下：

#### 模型 Bounding Box

| 参数 | 范围 | 尺寸 |
|------|------|------|
| X（宽度） | [−0.933, +0.933] | 1.866m |
| Y（长度） | [−3.560, +0.837] | 4.397m |
| Z（高度） | [0.000, +1.939] | 1.939m |

> 模型原点在前轴中心地面投影。车头（+Y）方向前悬 = +0.837m。

#### A 柱基部实测（Edit Mode 顶点坐标）

在 Blender Edit Mode 中，点选左侧 A 柱基部与前车门交汇处（窗框下沿角点）的顶点：

| 分量 | 实测值 | 说明 |
|------|--------|------|
| X | −0.805m | A 柱**内侧**面（朝向车内/玻璃侧） |
| Y | **−0.715m** | 前轴后方 715mm，A 柱基部 ✅ |
| Z | **1.319m** | 窗框下沿离地高度 ✅ |

> 该顶点在 A 柱的玻璃侧内面。镜座安装在 A 柱的**外侧**（朝向车外），X 应取车身外边缘值。

### 1.2 安装点坐标公式

后视镜安装在 A 柱基部，即侧窗前下角处。安装基准点 $P_{\text{mount}}$ 的三个分量：

安装点三个分量均直接采用 Blender Edit Mode 实测值：

$$P_{\text{mount}} = (\pm 0.805,\ -0.715,\ 1.319)$$

| 分量 | 实测值 | 说明 |
|------|--------|------|
| X | ±0.805m | A 柱表面在窗框高度的横向位置（左负右正） |
| Y | −0.715m | 前轴后方 715mm，A 柱基部 |
| Z | 1.319m | 窗框下沿离地高度 |

#### 安装点汇总

| 侧别 | $P_{\text{mount}}$ |
|------|-------------------|
| 左 (LEFT) | (−0.805, −0.715, 1.319) |
| 右 (RIGHT) | (+0.805, −0.715, 1.319) |

### 1.3 镜臂偏移 → 镜面中心

镜面中心 = 安装点 + 镜臂偏移向量。镜臂将镜面向三个方向偏移：

| 偏移分量 | 方向 | 含义 |
|----------|------|------|
| `arm_length` | 向车外侧 (±X) | 镜面伸出车身 |
| `arm_forward` | 向前 (+Y) | 镜面略微前倾 |
| `glass_height` | 向上 (+Z) | 镜面比安装点略高 |

公式：

$$P_{\text{glass}} = P_{\text{mount}} + \begin{pmatrix} s \cdot L_{\text{arm}} \\ d_{\text{fwd}} \\ d_{\text{up}} \end{pmatrix}$$

其中 $s = -1$（左侧）或 $s = +1$（右侧）。

镜臂偏移参数（基于实车数据和 ECE R46 法规重新计算）：

**设计依据**：
- SUV 标准镜面超出车身约 100mm（[实车数据](https://www.elementownersclub.com/threads/width-of-side-view-mirrors.12439/)）
- 拖车镜需看过最宽房车（2.5m），超出车身约 370mm
- 广角副镜/盲点镜位于主镜下方或外下角
- 驾驶员眼点估计：$P_{\text{eye}} = (-0.35, -1.0, 1.40)$

**目标点（基于 [ECE R46 Class III](https://eur-lex.europa.eu/legal-content/EN/TXT/HTML/?uri=CELEX:42014X0808(02))）**：
- 主镜（standard / towing_main / electric_main）：左 (−2.5, −15, 0.5)，右 (+3.0, −20, 0.5)
- 广角副镜（towing_wide_angle / electric_sub）：左 (−1.5, −4, 0.3)，右 (+1.5, −4, 0.3)
- 盲点镜（clip_on）：左 (−1.5, −5, 0.5)，右 (+1.5, −5, 0.5)

### 1.4 所有镜面类型：偏移量与角度

#### 偏移量汇总（相对于安装点）

| 镜面类型 | 侧别 | $\Delta X$ | $\Delta Y$ | $\Delta Z$ | 超出车身 | 说明 |
|---------|------|-----------|-----------|-----------|---------|------|
| standard_convex | L | −0.228 | +0.020 | +0.030 | 100mm | 标准主镜 |
| standard_convex | R | +0.228 | +0.020 | +0.030 | 100mm | |
| towing_main | L | −0.495 | +0.030 | +0.050 | 367mm | 拖车主镜，长臂 |
| towing_main | R | +0.495 | +0.030 | +0.050 | 367mm | |
| towing_wide_angle | L | −0.495 | +0.030 | −0.030 | 367mm | 同臂，主镜下方 |
| towing_wide_angle | R | +0.495 | +0.030 | −0.030 | 367mm | |
| electric_main | L | −0.228 | +0.020 | +0.030 | 100mm | 与 standard 同位 |
| electric_main | R | +0.228 | +0.020 | +0.030 | 100mm | |
| electric_sub | L | −0.228 | +0.020 | −0.030 | 100mm | 主镜下方 |
| electric_sub | R | +0.228 | +0.020 | −0.030 | 100mm | |
| clip_on_blind_spot | L | −0.238 | +0.020 | +0.010 | 110mm | 贴在标准镜外下角 |
| clip_on_blind_spot | R | +0.238 | +0.020 | +0.010 | 110mm | R=75mm |

#### 角度汇总（Blender Euler XYZ，度）

| 镜面类型 | 侧别 | rx | ry | rz |
|---------|------|----|----|-----|
| standard_convex | L | 89.7 | 0.0 | 30.0 |
| standard_convex | R | 90.3 | 0.0 | −35.9 |
| towing_main | L | 91.1 | 0.0 | 33.5 |
| towing_main | R | 91.0 | 0.0 | −37.1 |
| towing_wide_angle | L | 96.5 | 0.0 | 34.9 |
| towing_wide_angle | R | 98.5 | 0.0 | −38.9 |
| electric_main | L | 89.7 | 0.0 | 30.0 |
| electric_main | R | 90.3 | 0.0 | −35.9 |
| electric_sub | L | 95.0 | 0.0 | 29.6 |
| electric_sub | R | 98.2 | 0.0 | −35.8 |
| clip_on_blind_spot | L | 93.4 | 0.0 | 30.4 |
| clip_on_blind_spot | R | 95.4 | 0.0 | −36.2 |

> **角度规律**：
> - rx ≈ 90°（镜面近似竖直），广角/盲点镜略向下倾（rx > 90°）
> - ry ≈ 0°（无侧倾）
> - rz：左镜 +30°~+35°，右镜 −36°~−39°（右镜偏航更大，因为离驾驶员更远）

---

## 2. 镜面朝向推导

### 2.1 真实后视镜朝向参考

网络检索结果（[SAE BGE 方法](https://99percentinvisible.org/article/seeing-blind-spots-clever-trick-properly-align-cars-side-view-mirrors/)、[FMVSS 111](https://www.law.cornell.edu/cfr/text/49/571.111)、[ECE R46 Rev.6](https://www.unece.org/fileadmin/DAM/trans/main/wp29/wp29regs/2016/R046r6e.pdf)）：

| 参数 | 典型范围 | 来源 |
|------|---------|------|
| 镜面法向量偏航角（Yaw） | 15°~ 25°（偏离正后方） | SAE BGE / 工程测量 |
| 镜面法向量俯仰角（Pitch） | ±10° 以内，SUV 略下倾 | 调整经验 / 专利文献 |
| 最小水平 FOV | ≥ 20° | FMVSS 111 |
| 凸面镜对称轴倾角 | ≈ 8.5° | 专利 US4629296 |

> ECE R46 定义**最低视野覆盖范围**（地面区域），而非固定的镜面角度。实际角度取决于车辆几何和驾驶员眼点位置。

### 2.2 反射定律向量形式

**问题**：已知镜面中心 $P_{\text{glass}}$、驾驶员眼点 $P_{\text{eye}}$、目标点 $P_{\text{target}}$，求镜面法向量 $\hat{n}$，使驾驶员通过镜面能看到目标点。

**反射定律**：入射角 = 反射角。即入射光线与法向量的夹角等于反射光线与法向量的夹角。

设：
$$\hat{d}_{\text{eye}} = \frac{P_{\text{eye}} - P_{\text{glass}}}{|P_{\text{eye}} - P_{\text{glass}}|}$$
$$\hat{d}_{\text{target}} = \frac{P_{\text{target}} - P_{\text{glass}}}{|P_{\text{target}} - P_{\text{glass}}|}$$

由反射定律，法向量是两个方向的**角平分线**：

$$\hat{n} = \frac{\hat{d}_{\text{eye}} + \hat{d}_{\text{target}}}{|\hat{d}_{\text{eye}} + \hat{d}_{\text{target}}|}$$

**直觉理解**：镜面法向量恰好在"看向眼睛"和"看向目标"两个方向的正中间，这样反射定律自然满足。

### 2.3 法向量 → 欧拉角

Blender 中平面的默认法向量为 $\hat{z} = (0, 0, 1)$（朝上）。需要计算从 $\hat{z}$ 旋转到 $\hat{n}$ 的旋转角。

步骤：
1. 计算四元数 $q$ 使得 $q \cdot \hat{z} = \hat{n}$（使用 `Vector.rotation_difference()`）
2. 将四元数转换为 XYZ 欧拉角

```python
default_normal = Vector((0, 0, 1))
rotation = default_normal.rotation_difference(mirror_normal)
euler = rotation.to_euler("XYZ")
```

### 2.4 输入量

#### 驾驶员眼点 $P_{\text{eye}}$

> **TODO**：需要在 Blender 中实测。暂用估计值：

$$P_{\text{eye}} = (-0.35, -0.10, 1.20)$$

> X = −0.35：驾驶员在中线左侧 350mm（LHD）
> Y = −0.10：前轴后方 100mm（方向盘大致位置）
> Z = 1.20：座椅高度 + 躯干（略低于窗框下沿）

#### 默认目标点 $P_{\text{target}}$

$$P_{\text{target}} = (0, -20, 0.5)$$

> 车正后方 20m、离地 0.5m（相邻车道车辆车身高度）

### 2.5 CR-V 数值算例（standard_convex 左镜）

```
P_glass  = (-0.955, -0.695, 1.369)
P_eye    = (-0.35,  -0.10,  1.20)     ← 待实测替换
P_target = ( 0,     -20,    0.50)

Step 1: 方向向量
  d_eye    = P_eye - P_glass    = (0.605, 0.595, -0.169)
  |d_eye|  = 0.867
  d̂_eye    = (0.698, 0.686, -0.195)

  d_target = P_target - P_glass = (0.955, -19.305, -0.869)
  |d_target| = 19.334
  d̂_target = (0.049, -0.999, -0.045)

Step 2: 法向量（角平分线）
  sum      = d̂_eye + d̂_target = (0.747, -0.313, -0.240)
  |sum|    = 0.849
  n̂        = (0.880, -0.369, -0.283)

Step 3: 验证角度
  与 -Y 轴（正后方）的偏航角 ≈ arctan(0.880/0.369) ≈ 67°
  → 这是法向量方向，不是视线方向。
  法向量与正后方的夹角 ≈ arccos(0.369) ≈ 68°
```

> ⚠ 以上数值基于估计的眼点，仅用于验证公式逻辑。需实测眼点后重新计算。

### 2.6 与真实角度范围对比

_待眼点实测后重新计算并对比_

---

## 3. 镜面可视范围推导

### 3.1 凸面镜视场角

_待推导_

### 3.2 可视区域地面投影

_待推导_

### 3.3 曲率半径对视野的影响

_待推导_

### 3.4 CR-V 数值算例

_待计算_

---

## 4. Blender 实施验证

### 4.1 放置结果

_待实施_

### 4.2 用户确认

_待确认_

### 4.3 渲染验证

_待验证_
